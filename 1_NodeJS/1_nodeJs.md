# NodeJs is runtime env(environment) for js

# NodeJs = v8 engine embedded with c++
 
# NodeJS 
--> you can run js outside browser 
--> js can talk to native machine because of c++
--> you can create webservers in js language

# during embedding v8 engine with c++ all DOM related and window related elements are removed only core functionality of JS keepted 